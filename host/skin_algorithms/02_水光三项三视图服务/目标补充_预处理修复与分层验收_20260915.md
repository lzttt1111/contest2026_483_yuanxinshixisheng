# 当前目标补充：预处理修复与分层验收

用户于2026-09-15追加授权。本文件补充当前独立水光三项修复目标，不缩减原目标，不代表验收完成。

## 本阶段范围

- 只修改独立水光沙箱及对应水光交付包，不修改冻结V2、医生V3或06号Skill沙箱。
- 修复正常皮肤被误判毛发及重复扩张问题，检查法令纹和鼻下条带的下游影响；不以关闭所有排除代替修复。
- 先保存Git检查点，保留每轮候选差异和结果，支持回退到修复前代码。
- 只验收固定one/two两组三视图，暂不重跑1000例，也不扩大图库。
- 逐层出图：原始分析图、候选皮肤、五官排除、毛发识别证据、毛发扩张、最终有效皮肤、各检测器实际分析范围、最终检测图。
- 比较毛孔、可见色斑、表面油光的数量、有效面积、原始评分指标、整体分及区域分与等级。
- 新预处理与旧参考不匹配时，同参考分数差异仅用于诊断，必须明确标注，不能发布为重新标定的正式分数。需要重跑参考时再等待用户确认。

## 验收要求

1. 对照全部六张实际照片，不用单张成功代表两组通过。
2. 明确真实毛发保留抑制情况与正常皮肤恢复情况；不能用Mask面积或召回数量增加替代视觉验收。
3. 各层Mask同坐标、同输入、可追溯，不能用扩大前端显示轮廓掩盖分析域空洞。
4. 正式任务只显式运行pores、spots、surface_gloss，预处理模型独立说明，不暗中运行Brown、UV、痤疮或皱纹。
5. 用户看过分层图和检测/评分差异后，再决定参考重建与正式发布。此之前不得标记整体目标完成。

## 已核实的旧问题

9月9日当前水光对话：10:30要求检查脸颊毛发误排，10:54否决27张过程图中的多个分支，要求严重问题备案。

9月15日逐像素对照：two当前分析图、关键点、最终公共皮肤Mask均与9月9日未修复记录一致。固定眉间几何窗口19,035像素，候选皮肤19,035，毛发安全域覆盖19,035，最终皮肤0。该数值不等于全脸误删率。

旧量化导出会省略小于100像素的原生区域。合并区域的数量守恒只能解决字段完整性，不能恢复上游误删皮肤；禁止把两者混为一项修复。
