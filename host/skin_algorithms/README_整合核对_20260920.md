# 参赛源码整合核对

本目录是 2026-09-20 重新核对后的未压缩参赛源码目录。

## 三个模块

- `01_V2单双输入十二项稳定版`：来源为内网 `dermavision/dev`，提交 `787a500d35223aebcb3bcf42eb98ee82fecac73c`，包含单 RGB、机构四光源、十二项检测、评分、Worker 和报告代码。
- `02_水光三项三视图服务`：来源为内网 `shuiguang-test/dev`，提交 `d7c84c18b859be60967a3172266aedcac36f9db1`，接收三张 RGB 三视图，返回毛孔、可见色斑和表面油光的整体及分区评分 JSON。
- `03_目标检测与自动拍照`：沿用已验收的 PC 目标检测、角度检测和自动拍照源码快照，负责三视图采集，不承担水光三项评分。

## 提交前核对

根目录的 `PACKAGE_MANIFEST.json`、`SUBMISSION_RECEIPT.json` 和本文件旁的 `SOURCE_INTEGRATION_RECEIPT_20260920.json` 用于核对文件完整性、源分支和提交号。模块内的技术文档已经同步加入本次整合的源提交说明。

本目录没有外层压缩包。提交时直接提交整个目录及三个模块子目录。
