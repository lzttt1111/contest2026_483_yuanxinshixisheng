# 0.1.3 强审问题修复对应表

仅修复开发Skill检查器、合同与测试，不修改任何业务算法、训练数据、正式结果或日志采集。

| 0.1.2报告编号 | 修改 |
|---|---|
| P2-01 | 非空帧/时钟身份、非空字段列表；未知身份拒绝点迁移批准 |
| P2-02 | layout/color允许值与非空provider；缺语义不能成为有效数值比较 |
| P2-03 | 显式单位集合，所有支持的物理单位均需要正标尺 |
| P2-04 | 状态枚举，未知状态不产生恢复计划；非确认终态保留旧执行者检查 |
| P2-05 | mask_edited/human_edited/protected与确认状态共同保护；空记录ID拒绝 |
| P2-06 | spec列表/对象、capture槽位/帧结构先验证；所有CLI统一结构化输入和输出异常 |
| P3-01 | 差值溢出拒绝；RMSE缩放计算；分位数加权插值；输出序列化进入异常边界 |
| P3-02 | 增加显式artifact_sha256绑定；未指定时明确未检查绑定，不接受任意投影字段自动证明资产 |

测试：tests/xhigh_regressions.py 37项在Windows、Linux运行；包括错误绑定拒绝和正确绑定通过的对照。原30、52、12、12项回归继续运行，Linux另搬迁30项。数值极端输入可返回正确有限值，不要求一律拒绝。

适用限制不变：帧真实性/唯一性、时钟实例身份、真实标注状态、物理尺度来源、真实租约和完整产品效果由上游提供及单独验收。合同快照通过不是上述事实的证明；新Skill没有回填历史使用记录。
