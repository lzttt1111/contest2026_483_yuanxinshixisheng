# 完整产物示例：人工标注保护

合成样本a为positive，归一化xywh=[0.1,0.1,0.5,0.6]，人工确认revision=2；b为negative、空框且已确认。本次rerun_ids为空。

| 样本 | 框范围 | 标签检查 | 人工状态 | 处理 |
|---|---|---|---|---|
| a | x:0.1–0.6，y:0.1–0.7 | 正例有框 | revision2已确认 | 保留 |
| b | 无框 | 负例空框 | 已确认 | 保留；缺版本演进信息单列 |

标签合同：框采用xywh而非xyxy；Mask、点、框和预览必须共享同一revision。质量检查通过只说明清单一致，真实图片内容还需审核。

修订记录示例：不新增revision、不重算标签，安全重跑列表为空。若拟重跑未知ghost，结果转为fail且安全列表仍为空；若a人工状态缺失，不按False处理。自动建议单独保留，不能覆盖最终人工层。
