# 完整产物示例：重拍后的结果关联

生产端字段capture_id/revision/frame_id，消费者需要capture_id/revision；当前revision=3，旧分析revision=2；源帧10、当前帧12，未验证配准。两时钟分别是device-a:boot1与device-b:boot1，尚无映射。

接口适配结论：字段名虽足够，但旧revision不能参与当前点显示；不同帧点坐标不能凭业务ID直接叠加；时钟域差异须实际映射。文档如声称模式切换保留照片，而代码调用reset(all)，应以核实的实现为依据并修订方案。

交付物组织：接口映射表、状态清空规则、结果索引、模型/版本清单、可运行入口、失败/重试测试和接手说明。验收先完成同帧同版本业务场景，再单独验证配准；不能将空间人脸锁定写成身份认证。

当前项目参考包两槽与水光三图不等价，此Skill应先指出缺口，不为展示强行声明已整合成功。
